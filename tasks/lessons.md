# Leçons — Léa Beauté

| Date       | Ce qui s'est mal passé                                                                 | Règle à suivre                                                                                          |
|------------|----------------------------------------------------------------------------------------|---------------------------------------------------------------------------------------------------------|
| 2026-09-09 | Domaine cherché : `leabeautevalogne.fr` → NXDOMAIN. Le vrai domaine est `leabeautevalognes.fr` (avec « s ») | Toujours confirmer le domaine de production depuis le code (`SEO.jsx`, `sitemap.xml`, `.htaccess`) avant de tester |
| 2026-09-09 | Diagnostic initial faux : « toute l'API renvoie du HTML ». C'était le SPA du domaine principal, alors que le front appelle `api.leabeautevalognes.fr` | Lire l'URL d'API réellement embarquée dans le **bundle déployé** avant de conclure, pas seulement le code source local |
| 2026-09-09 | `/api/google-reviews` renvoie 200 avec un JSON de repli factice (`rating: 4.8`, 0 avis) : l'échec est indiscernable d'un succès | Ne jamais renvoyer 200 + données inventées sur un chemin d'erreur. Distinguer explicitement les causes et les journaliser |
| 2026-09-09 | Le code utilise l'API Google Places **legacy**, fermée aux nouveaux projets depuis mars 2025 | Vérifier le cycle de vie d'une API tierce avant de diagnostiquer une clé « invalide »                     |
| 2026-09-09 | Helper de test `withGoogleEnv` en `try/finally` synchrone autour d'un `run()` async : les variables d'env étaient restaurées avant la résolution de la promesse, faisant échouer les tests de cache | Un helper qui encadre du code asynchrone doit être `async` et faire `return await run()` |
| 2026-09-09 | `npm run build` lancé depuis un arbre de travail en retard sur `HEAD` (129 suppressions non commitées) : aurait supprimé le pré-rendu SSG et le `.htaccess` canonique | Toujours lancer `git status` avant un build de production et refuser de builder si l'arbre diverge de `HEAD` |
| 2026-09-10 | J'ai attribué l'écart de l'arbre de travail à une « synchronisation partielle de Dev_Sync ». **Faux** : le `.stignore` de Syncthing n'exclut que `node_modules`, `dist`, `build`, caches et `.DS_Store`. La vraie cause était un `git clone` + `git reset` (au lieu d'un `checkout`) le 10/06 sur un dossier contenant une copie plus ancienne | Ne pas désigner une cause sans preuve. Lire `git reflog` et le fichier d'exclusion de l'outil de sync **avant** d'accuser la synchronisation |
| 2026-09-10 | J'ai présenté une modification de `frontend/.env.example` comme une « preuve en direct » d'un écrasement par Syncthing. C'était en réalité une édition d'Éric | Ne pas conclure à une cause automatique parce qu'elle confirme l'hypothèse en cours. Demander avant d'attribuer un changement à un outil |
| 2026-09-10 | `.git` était synchronisé par Syncthing entre le MacBook et l'iMac, sans versioning : historique et fichiers de travail pouvaient diverger sans récupération possible | Un dépôt git se synchronise via son remote, jamais via un outil de synchronisation de fichiers. Exclure `**/.git` du `.stignore` — **sans** le préfixe `(?d)`, qui autorise la suppression |
| 2026-09-11 | Un `POST` d'API échouait sans aucune réponse ; j'ai d'abord suspecté le CORS applicatif, Stripe, puis le schéma SQL. La cause était un challenge 307 de Tiger Protect (o2switch), déclenché par le **User-Agent navigateur** — invisible en `curl` avec l'UA par défaut | Devant une requête sans réponse, rejouer la requête **avec les en-têtes exacts du navigateur** (User-Agent, Origin, Referer, Sec-Fetch-*). Un écart de comportement entre curl et le navigateur pointe la couche sécurité de l'hébergeur, pas l'application |
